#include <stdio.h>
/**
 * main - function
 * Return: 0
 */

int main(void)
{
int x, c;

for (x = '0'; x <= '9'; ++x)
{
putchar(x);
}
for (c = 'a'; c <= 'f' ; ++c)
{
putchar(c);
}
{
	putchar('\n');
}
return (0);
}
